//what is react
// when to use react
// adv of react
//Advantages of Using React Over Alternative Frameworks 


//diadd
//Load on client side renduring
//lack of Seo(search enging)
//form validation & handling not efficient when compare to angular
1 .What is React?

React is a JavaScript library used for building user interfaces (UI), especially for single-page applications (SPAs). It helps developers create fast, interactive, and scalable web applications efficiently.
Key Features of React

✅ Component-Based – UI is broken into reusable components.
✅ Virtual DOM – Efficiently updates only the necessary parts of the UI.
✅ One-Way Data Binding – Ensures predictable state management.
✅ JSX (JavaScript XML) – Allows writing HTML inside JavaScript.
✅ Hooks – Enables state and lifecycle management without classes.
✅ Fast Rendering – Uses a diffing algorithm for optimal performance.

Why Use React?
🔹 Faster development
🔹 Reusable components
🔹 Large community & ecosystem
🔹 Works well with frameworks like Next.js
🔹 Used by big companies (Facebook, Instagram, Netflix, etc.)


2. When to Use React?

React is ideal when you need:
1️⃣ Building Single-Page Applications (SPAs)

👉 If your web app needs fast navigation without full-page reloads, React is perfect.
✅ Example: Gmail, Facebook, Trello
2️⃣ Reusable Components

👉 If your project has repetitive UI elements (buttons, cards, forms), React’s component-based architecture helps.
✅ Example: E-commerce product cards, dashboards
3️⃣ Interactive User Interfaces

👉 When your app requires dynamic updates (like live search, real-time updates), React optimizes rendering.
✅ Example: Chat apps, stock market dashboards
4️⃣ Large-Scale Applications with State Management

👉 If your app needs complex state management, React with Redux/Zustand is a great choice.
✅ Example: Banking apps, large SaaS platforms
5️⃣ Cross-Platform Development

👉 If you want to build both web and mobile apps using the same knowledge, React (with React Native) is useful.
✅ Example: Airbnb, Instagram (React Native)
6️⃣ When SEO Optimization is Required

👉 If you need SEO-friendly apps, you can use Next.js (built on React) for server-side rendering (SSR).
✅ Example: News websites, blogs, e-commerce (Amazon-like apps)
When NOT to Use React?

❌ Simple static websites → Use HTML, CSS, Bootstrap.
❌ Heavy real-time gaming apps → Use Unity or Three.js.
❌ Performance-intensive applications → Consider frameworks like Svelte.

3 . Advantages of React 🚀
1️⃣ Component-Based Architecture

✅ Reusable Components – Write once, use anywhere.
✅ Easier Maintenance – Break UI into smaller, manageable pieces.
✅ Faster Development – Reduces redundant code.
2️⃣ Virtual DOM for Fast Performance

✅ Efficient Updates – Only updates necessary parts of the UI.
✅ Better User Experience – No full-page reloads.
✅ Optimized Rendering – Uses a diffing algorithm for minimal changes.
3️⃣ One-Way Data Binding (Predictable State Management)

✅ Unidirectional Data Flow – Prevents unexpected UI changes.
✅ Better Debugging – Easy to track data changes.
✅ Works well with Redux, Zustand, Context API for state management.
4️⃣ Uses JSX for Easy Development

✅ Write HTML inside JavaScript – Improves code readability.
✅ Less Boilerplate Code – More intuitive syntax.
✅ Better Component Composition – Easier UI structuring.
5️⃣ Rich Ecosystem & Strong Community Support

✅ Lots of Third-Party Libraries – Like Material-UI, Ant Design, Redux.
✅ Huge Community – Solutions available for almost any issue.
✅ Backed by Meta (Facebook) – Regular updates and improvements.
6️⃣ Cross-Platform Development (React Native)

✅ Write Once, Run Anywhere – Web + Mobile with similar logic.
✅ Cost & Time Saving – Single codebase for Android & iOS.
7️⃣ SEO-Friendly with Next.js

✅ Server-Side Rendering (SSR) – Faster page loads.
✅ Static Site Generation (SSG) – Pre-rendered pages for SEO.
8️⃣ Easy Integration with Other Technologies

✅ Can work with TypeScript, GraphQL, Node.js, and Firebase.
✅ Can be integrated with legacy codebases or microservices.

keys in react?

i)Keys are significant in React because they aid in determining whether items in a list have been changed, updated, or removed. This process helps React to optimize the rendering by recycling(reconstitution) existing DOM elements.
ii)This is why keys need to be stable an unique in a list.
    
Expo setup

 Install Expo CLI (Recommended for Beginners) [rncs]-shortcut

I .react native cli:

npx @react-native-community/cli init YourProjectName 
npx @react-native-community/cli@latest init Name - latest 
npm i
  
add: cd android
1 nano local.properties
2 sdk.dir=C:\\Users\\harip\\AppData\\Local\\Android\\sdk
3 ctr+x and Y and Enter
cd
npx react-native run-android - first time 
npm start --reset-cache - second time

npx react-native run-ios

II. expo cli:
[npm install -g expo-cli] 
[npx create-expo-app@latest] - latest
npx create-expo-app MyApp

remove node module:
rm -rf node_modules android/.gradle





cd MyApp

 Android file
npx expo run:android




npm start or  npx expo start [android -  npm run android ]

check Box 
npx expo install expo-checkbox*-
import Checkbox from 'expo-checkbox';

svg - npm install react-native-svg
curosel  - npm install react-native-snap-carousel


 const [values, setValues] = useState([]);
    const [keys, setKeys] = useState([]);
    useEffect(() => {
        const xhr = new XMLHttpRequest;
        xhr.open("GET", "http://localhost:5173/data.json", true)
        xhr.onload = () => {
            const data = JSON.parse(xhr.responseText);
            setValues(data.users);
            setKeys(Object.keys(data.users[0]));
        }
        xhr.send()
    }, [])



Image Picker:

npx expo install expo-image-picker

import * as ImagePicker from 'expo-image-picker';
import * as Permissions from 'expo-image-picker';


Secure Store :

import * as SecureStore from 'expo-secure-store';

// Store data securely
const saveSecureData = async (key, value) => {
  await SecureStore.setItemAsync(key, value, { keychainAccessible: SecureStore.WHEN_UNLOCKED });
};

// Retrieve data
const getSecureData = async (key) => {
  return await SecureStore.getItemAsync(key);
};

// Delete stored data
const deleteSecureData = async (key) => {
  await SecureStore.deleteItemAsync(key);
};


MMkV store :

import { MMKV } from 'react-native-mmkv';

const storage = new MMKV();

// Store encrypted data
storage.set("secureKey", "mySecretData");

// Retrieve encrypted data
const value = storage.getString("secureKey");

// Remove data
storage.delete("secureKey");

Formik:

npm install formik yup
type - 1(yutube)
npm install formik yup react-native-elements react-native-vector-icons
import React, { useState } from 'react';
import { View, Button, StyleSheet, ScrollView } from 'react-native';
import { Input, Icon } from 'react-native-elements';
import { useFormik } from 'formik';
import * as Yup from 'yup';

const SignUp = () => {
  const [showPassword, setShowPassword] = useState(false);

  const formik = useFormik({
    initialValues: {
      userName: '',
      email: '',
      phone: '',
      password: '',
      role: '',
    },
    validationSchema: Yup.object().shape({
      userName: Yup.string()
        .required('This field is required')
        .min(5, 'Please enter a valid username'),
      email: Yup.string()
        .required('This field is required')
        .matches(
          /^[^<>()[\]\\.,;:\s@"]+@[^<>()[\]\\.,;:\s@"]+\.[^<>()[\]\\.,;:\s@"]+$/,
          'Please enter a valid email'
        ),
      phone: Yup.string()
        .required('This field is required')
        .matches(/^[0-9]{10,12}$/, 'Please enter a valid phone number'),
      password: Yup.string()
        .required('This field is required')
        .min(6, 'Password must be at least 6 characters'),
    }),
    onSubmit: values => {
      console.log(values);
    },
  });

  return (
    <ScrollView contentContainerStyle={styles.container}>
      {/* Username */}
      <Input
        label="Username"
        value={formik.values.userName}
        onChangeText={formik.handleChange('userName')}
        onBlur={formik.handleBlur('userName')}
        errorMessage={
          formik.errors.userName && formik.touched.userName
            ? formik.errors.userName
            : ''
        }
        labelStyle={styles.label}
        inputContainerStyle={styles.inputContainer}
        inputStyle={styles.input}
      />

      {/* Email */}
      <Input
        label="Email"
        value={formik.values.email}
        onChangeText={formik.handleChange('email')}
        onBlur={formik.handleBlur('email')}
        errorMessage={
          formik.errors.email && formik.touched.email
            ? formik.errors.email
            : ''
        }
        labelStyle={styles.label}
        inputContainerStyle={styles.inputContainer}
        inputStyle={styles.input}
      />

      {/* Phone */}
      <Input
        label="Phone"
        value={formik.values.phone}
        onChangeText={formik.handleChange('phone')}
        onBlur={formik.handleBlur('phone')}
        errorMessage={
          formik.errors.phone && formik.touched.phone
            ? formik.errors.phone
            : ''
        }
        labelStyle={styles.label}
        inputContainerStyle={styles.inputContainer}
        inputStyle={styles.input}
        keyboardType="phone-pad"
      />

      {/* Password */}
      <Input
        label="Password"
        value={formik.values.password}
        onChangeText={formik.handleChange('password')}
        onBlur={formik.handleBlur('password')}
        errorMessage={
          formik.errors.password && formik.touched.password
            ? formik.errors.password
            : ''
        }
        secureTextEntry={!showPassword}
        labelStyle={styles.label}
        inputContainerStyle={styles.inputContainer}
        inputStyle={styles.input}
        rightIcon={
          <Icon
            type="ionicon"
            name={showPassword ? 'eye' : 'eye-off'}
            size={20}
            color="grey"
            onPress={() => setShowPassword(!showPassword)}
          />
        }
      />

      {/* Submit */}
      <Button title="Sign Up" onPress={formik.handleSubmit as any} />
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  label: {
    fontSize: 14,
    fontWeight: 'normal',
    color: 'grey',
    paddingBottom: 5,
  },
  inputContainer: {
    height: 45,
    borderWidth: 1,
    borderColor: 'grey',
  },
  input: {
    paddingHorizontal: 15,
    fontSize: 14,
  },
});

export default SignUp;

type - 2

import React from 'react';
import { View, TextInput, Button, Text, StyleSheet } from 'react-native';
import { Formik } from 'formik';

const LoginForm = () => {
  return (
    <Formik
      initialValues={{ email: '', password: '' }}
      onSubmit={(values) => {
        console.log(values);
      }}
      validate={(values) => {
        const errors: any = {};
        if (!values.email) {
          errors.email = 'Required';
        } else if (!/\S+@\S+\.\S+/.test(values.email)) {
          errors.email = 'Invalid email address';
        }

        if (!values.password) {
          errors.password = 'Required';
        }

        return errors;
      }}
    >
      {({ handleChange, handleBlur, handleSubmit, values, errors, touched }) => (
        <View style={styles.container}>
          {/* Email Input */}
          <TextInput
            style={styles.input}
            placeholder="Email"
            onChangeText={handleChange('email')}
            onBlur={handleBlur('email')}
            value={values.email}
          />
          {touched.email && errors.email && <Text style={styles.error}>{errors.email}</Text>}

          {/* Password Input */}
          <TextInput
            style={styles.input}
            placeholder="Password"
            secureTextEntry
            onChangeText={handleChange('password')}
            onBlur={handleBlur('password')}
            value={values.password}
          />
          {touched.password && errors.password && <Text style={styles.error}>{errors.password}</Text>}

          {/* Submit Button */}
          <Button onPress={handleSubmit} title="Login" />
        </View>
      )}
    </Formik>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 10,
    marginBottom: 10,
  },
  error: {
    color: 'red',
    fontSize: 12,
    marginBottom: 10,
  },
});

export default LoginForm;


git merge , rebae, squash

graph -  git log --graph --oneline --decorate
previous branch - git checkout -
